/**
 * Browser-half host stub for the search surface. The real registration lives in
 * `./client` so type-only consumers (and the bundler) can resolve the surface
 * without pulling the React tree into the host entry.
 * @module @deepseek-ai/dsh-client-ui-search
 */
export {}
